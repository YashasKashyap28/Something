[exec_policy]
mode = "full"
allowed_commands = [
    "systemctl",
    "journalctl",
    "bash",
    "sh"
]
safe_bins = [
    "systemctl",
    "journalctl",
    "bash",
    "sh"
]

[exec_policy]
mode = "Allowlist"
safe_bins = ["systemctl"]
